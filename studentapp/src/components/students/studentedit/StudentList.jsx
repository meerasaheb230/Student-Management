
// import { Student } from "../SingleStudent/SingleStudent";

// export const StudentList = ({ students, handleStudentDelete }) => {
//   const onStudentDelete = (studentId) => {
//     handleStudentDelete(studentId);
//   };
//   return (
//     <ul className="todo-list">
//       {students.map((student) => (
//         <Student student={students} key={student._id} onStudentDelete={onStudentDelete} />
//       ))}
//     </ul>
//   );
// };